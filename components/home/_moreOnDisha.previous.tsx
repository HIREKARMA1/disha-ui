/**
 * Snapshot helper for UI undo.
 * "Previous" = revert the most recent change in the chat (any section), not only More on Disha.
 */
export const PREVIOUS_MEANS = 'Revert the latest change only.'
